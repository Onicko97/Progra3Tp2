package logic;

import models.grafo.Grafo;

public class MST<T> {
	
	public void prim(Grafo<T> g, T inicio) {
		int verticesTotales = g.getVertices().size();
		Grafo<T> arbolGeneradorMinimo = new Grafo<T>(null);
	}
}
